spring:
  application:
    name: alarm
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:alarm;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;MODE=MySQL
    #    url: jdbc:h2:tcp://localhost/~/trip;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;MODE=MySQL
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    defer-datasource-initialization: true

logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.springframework.web.client.RestTemplate: DEBUG

springdoc:
  swagger-ui:
    use-root-path: true
    enabled: true
  api-docs:
    enabled: true